spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Fitness
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

fitness:
  app:
    jwtSecret: bezKoderSecretKey
    jwtExpirationMs: 86400000

